# Frontend (Vite + React)

This folder contains a minimal Vite + React scaffold.

Quick start:

```powershell
cd frontend
npm install
npm run dev
```

If `npx` or `npm` are not available on your system, install Node.js from https://nodejs.org/ or use `pnpm` or `yarn`.
